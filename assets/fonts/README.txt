Currently using Google Fonts via CDN — see <link> in each HTML <head>.
To self-host: download woff2 files via google-webfonts-helper.herokuapp.com,
drop them here, then replace the Google <link> with local @font-face in main.css.
